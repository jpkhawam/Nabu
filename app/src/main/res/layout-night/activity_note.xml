<?xml version="1.0" encoding="utf-8"?>

<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:isScrollContainer="true"
        android:orientation="vertical">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/noteTopBar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?android:colorBackground"
                android:elevation="0dp"
                app:menu="@menu/note_top_menu"
                app:navigationContentDescription="@string/go_back_to_home_page"
                app:navigationIcon="@drawable/ic_back_dark" />

        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/textFieldTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:boxStrokeWidth="0dp">

            <EditText
                android:id="@+id/input_note_title"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fontFamily="@font/open_sans_semi_bold"
                android:hint="@string/title"
                android:importantForAutofill="no"
                android:inputType="text"
                android:textAlignment="viewStart"
                android:textAppearance="?attr/textAppearanceLabelLarge"
                android:textColorHint="?colorControlHighlight"
                android:textSize="20sp" />

        </com.google.android.material.textfield.TextInputLayout>

        <!--
              TODO:
                  1- The bottom bar jumps over the keyboard, wtf?
                  we could keep it, i added copy and paste so it makes sense?
                  2- Make screen scroll past the title
                  3- It is impossible to know where to press to enter title and content
                  but having borders is kinda ugly
                                                                                -->

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/textFieldContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:isScrollContainer="true"
            app:boxStrokeColor="?android:colorBackground"
            app:boxStrokeWidth="0dp">

            <EditText
                android:id="@+id/input_note_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/noto_sans_regular"
                android:hint="@string/enter_your_note_content_here"
                android:importantForAutofill="no"
                android:inputType="text"
                android:textAlignment="viewStart"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textSize="16sp" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- display this on the bottom of the screen right above bottom bar
        <TextView
            android:id="@+id/note_date_edited"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Date edited: DD/MM/YYYY HH:MM"
            android:layout_gravity="bottom"
            /> -->

    </LinearLayout>

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomAppBar"
        style="@style/Widget.MaterialComponents.BottomAppBar.Colored"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginTop="20dp"
        android:layoutDirection="rtl"
        app:menu="@menu/note_bottom_menu"
        app:navigationIcon="@drawable/ic_more_dark"
        app:navigationIconTint="?colorControlNormal" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
